language: python
os: linux
branches:
  only:
    - main
    - dev
jobs:
  install: 
    - python -m pip install --upgrade pip
    - pip install -r requirements.txt
  script:
    - python blog/manage.py test
